#include "binaire.h"

using namespace std;

Binaire::Binaire(const string& nom, Expression* left, Expression* right) : Expression(nom), _opleft(left), _opright(right)
{
    // Reste des méthodes dans le .h définies
}

